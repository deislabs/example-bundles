{
    "name": "armbase",
    "version": "0.1.0",
    "invocationImages": [
        {
        "imageType": "docker",
        "image": "cnab/armbase:0.1.0"
        }
    ],
    "parameters": {
        "azure_resource_group": {
            "defaultValue": "armbase-default",
            "type": "string",
            "destination": {
                "env": "AZURE_RESOURCE_GROUP"
            }
        },
        "azure_location": {
            "defaultValue": "useast",
            "type": "string",
            "destination": {
                "env": "AZURE_LOCATION"
            }
        }
    },
    "credentials": {
        "tenant_id": {
            "env": "AZURE_TENANT_ID"
        },
        "client_id": {
            "env": "AZURE_CLIENT_ID"
        },
        "client_secret": {
            "env": "AZURE_CLIENT_SECRET"
        },
        "subscription_id": {
            "env": "AZURE_SUBSCRIPTION_ID"
        }
    }
}
